# SmartTrip frontend

React SPA。啟動、API 契約與三人分工請見 [根目錄 README](../README.md) 與 [共同規格](../docs/DEVELOPMENT_SPEC.md)。

需要 Node 22.12+ 與 pnpm 11+；使用 `pnpm install --frozen-lockfile` 安裝、`pnpm dev` 啟動。驗證：`pnpm build`、`pnpm lint`。
